@inherits LayoutComponentBase

@* Bare passthrough — each page owns its full shell via GACS.Components shells.
   AdminShell / GateShell / VisitorShell are used directly inside each @page component. *@
@Body
