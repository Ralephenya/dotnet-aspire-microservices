@* ============================================================
   Component1.razor — DELETE OR REPLACE THIS FILE

   This is the default Razor Class Library placeholder generated
   by the dotnet template. It is intentionally left here so the
   project compiles out of the box, but it has no purpose.

   When you add your first real shared component:
     1. Delete this file (right-click → Delete in Solution Explorer)
     2. Create your component under Components/ or Shells/
     3. Follow the naming convention:  PascalCase.razor
        with a matching:               PascalCase.razor.css
     4. Use a class prefix to namespace CSS (e.g. "rb-" for RiskBadge)

   See GACS.Components/Components/RiskBadge.razor for a worked example.
   ============================================================ *@
